"use strict";
/*import * as images from "../routes/api/images";

const filename = "image_five";
const width = 200;
const height = 500;

const filenameError = "image_seventeen";
const widthError = 200;
const heightError = 500;

beforeEach(async function() {
    await images.resizeImage(filename,width,height);
  });
  
  it('Expect file path with correct filename', async function() {
    const result = await images.resizeImage(filename,width,height);
    expect(result).toEqual(__dirname + `../` + `/route/api/images/resized_images/${filename}_${width}_${height}.jpg`);
  });

  beforeEach(async function() {
    await images.resizeImage(filenameError,widthError,heightError);
  });

  it("Expect an error of missing input", async ()=> {
    const result = await images.resizeImage(filenameError,widthError,heightError);
    expect(result).toEqual("Input file is missing, Please check input file existance and try again.");
  });*/
